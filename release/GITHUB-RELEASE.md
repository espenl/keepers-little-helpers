The materials tracker now matches the native game menus: a complete window background and header, the graphical close button, item icons, brighter names, and aligned **Have / Need** counts.

Drag the header to move it. The panel stays inside the HUD and remembers its position during the session, including when pinning another blueprint. Counts use carried inventory only.

Includes all four helpers from 0.3.4. Configure them under **Esc > Keeper's Little Helpers**.

Validated in-game with the creator; screenshot shows the final panel. Moving remains a limited preview: back up saves. Requires BepInEx 5.4.23.5 x64 separately. Close the game before replacing the plugin DLL.
